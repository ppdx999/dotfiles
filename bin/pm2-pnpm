#!/bin/sh

usage(){
  echo "Usage: pm2-yarn <command>" >&2
}

print_usage_and_exit(){
  usage
  exit 1
}

cwd=$(pwd | xargs basename)
cmd="$1"
case "$cmd" in '') print_usage_and_exit;; esac

pm2 start pnpm --name "$cmd-$cwd" -- "$@"
